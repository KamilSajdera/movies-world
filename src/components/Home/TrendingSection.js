import classes from "./TrendingSection.module.css";

const TrendingSection = ({ items }) => {
    console.log(items)
  return <div className={classes.trendingSection}></div>;
};

export default TrendingSection;
