const ResultsContent = ({ results, title }) => {
  console.log(results);
  return (
    <>
      <h2>
        Search results for: <b>{title}</b>
      </h2>
    </>
  );
};

export default ResultsContent;
